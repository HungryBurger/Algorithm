#include <iostream>
#include <vector>
#include <algorithm>
#include <string.h>
#include <string>

using namespace std;

vector <string>solution(vector<string>);
int main(void) {
	vector<int> input;
	int sum = 0;

	return 0;
}
vector < string> solution(vector<string> record) {
	vector<string>answer;
	return answer;
}